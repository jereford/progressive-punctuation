{{> header}}

    <body data-hijacking="on" data-animation="fixed">

    <section class="cd-section visible">
        <div>
            <div class="cd-section__wrapper">
                <div class="cd-section__container one">
                    <img src="imgs/pp-logo.png" class="splash-logo">
                    <h1>Thanks! You'll know exactly when we launch.</h1>
                </div>
            </div>
        </div>
    </section>

{{> footer}}
